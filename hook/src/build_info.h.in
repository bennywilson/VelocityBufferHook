#pragma once

// Generated by CMake's configure_file() from build_info.h.in - do not edit
// the generated header directly, edit this template instead.
//
// Captured at CMake CONFIGURE time, not at every build: re-run cmake (or
// just build - CMakeLists.txt below re-runs configure automatically when
// HEAD changes, see CONFIGURE_DEPENDS) to refresh it after committing.
//
// Real by construction rather than fabricated: git rev-parse fails loudly
// (CMake FATAL_ERROR) if this isn't a git checkout, rather than silently
// baking in a placeholder that would read as a real commit in the manifest.

namespace mv
{
inline constexpr const char* kBuildGitCommit = "@MV_GIT_COMMIT@";
}
